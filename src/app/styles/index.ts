import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  completed: {borderWidth: 2, borderColor: 'green'},
  failure: {borderWidth: 2, borderColor: 'red'},
});
export default styles;
